name: moneypot

services:
  app:
    container_name: MoneyPotApp
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "3001:3000"

    environment:
      - NODE_ENV=production

    extra_hosts:
      - "host.docker.internal:host-gateway"

    restart: always